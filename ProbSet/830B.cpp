#include <bits/stdc++.h>

using namespace std;

int main() {
	int n;
	cin >> n;
	int a[n], b[n];
	map<int, vector<int>> m;
	for (int i = 0; i < n; i++) {
		cin >> a[i];
		b[i] = a[i];
		m[]
	}
	sort(b, b+n);
	for (int i = 0; i < n; i++) {
		cout << a[i] << " " << b[i] << endl;
	}
	return 0;
}
